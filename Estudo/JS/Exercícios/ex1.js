function operacoes(x, y) {
    const soma = x + y
    const sub = x - y
    const mult = x * y
    const div = x / y

    console.log(`Adição: ${soma}, Subtração: ${sub}, Multiplicação: ${mult}, Divisão: ${div}`)
}

operacoes(4, 2)